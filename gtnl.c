#include <fcntl.h>
#include <math.h>
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>

char *get_next_line(int fd)
{
	char *tmp;
	while
	char * str =malloc(n * sizeof(char) +1);
	
	read(fd, str, n);
}